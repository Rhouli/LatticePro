/* Author: Ryan Houlihan
 *
 * Lammps Lattice dump file Producer
 *
 * Usage: LatPro [-FLAG] [ARGUMENT]
 * FLAG: 
 *       [-s fileName]                       :: read in an sph file to fit to the given lattice 
 *       [-d xMin xMax yMin yMax zMin zMax]  :: read in dimensions 
 *       [-o1 fileName]                      :: output to dump file 
 *       [-o2 fileName]                      :: output to data file
 *       [-r radiusVal]                      :: radius value 
 *       [-t]                                :: transform vectors 
 *       [-v radiHigh radiMid radiLow]       :: variable sized radius
 *
 */

#ifndef LATPRO_H
#define LATPRO_H

// Includes
#include "GrassPro.h"
#include "GranPro.h"

// Functions

#endif

